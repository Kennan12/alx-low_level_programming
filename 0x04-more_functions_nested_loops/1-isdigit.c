#include "main.h"

/**
 * _isdigit - check a characters
 * @c: char to be checked
 *
 * Return: 1 for a character that or 0 any else
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
	return (1);
	}
	return (0);
}
