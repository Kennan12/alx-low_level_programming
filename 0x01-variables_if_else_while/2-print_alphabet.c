#include <stdio.h>

/**
 * main - print lowercase alphabet
 * Return: 0 (success)
 */
int main(void)
{
	char k;

	k = 'a';
	while
		(k <= 'z') {
			putchar(k);
			k++;
		}
	putchar('\n')
	return (0);
}
