#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char k;
	int n;

	k = 'a';
	n = 0;
	while
		(n < 10) {
			putchar(n + '0');
			n++;
		}
	while
		(k <= 'f') {
			putchar(k);
			k++;
		}
	return (0);
}
